fips_begin_module(yakc_core)
    fips_vs_warning_level(3)
    fips_files(kc85.h memory.h z80.h common.h _dummy.cc)
    fips_generate(FROM opcodes.py HEADER opcodes.h)
    fips_generate(FROM roms.py)
fips_end_module()
